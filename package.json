{
  "name": "payermax-payment-demo",
  "version": "2.0.0",
  "description": "PayerMax 支付体验中心交互系统 V2.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd frontend && npm install"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "winston": "^3.19.0"
  }
}
